# 写作台内核 (deskcore/) 依赖 —— FastAPI + MCP on Railway。
# 上限锁到下一个 major (与 R-017 / librarian 同理念: 防 fresh install 拉到
# breaking major)。真要升 major 前: 看 changelog → staging smoke → 一次升一个。
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
supabase>=2.0,<3.0
anthropic>=0.40,<1.0
httpx>=0.27,<1.0
mcp>=1.2,<2.0
google-genai>=0.3,<2.0
