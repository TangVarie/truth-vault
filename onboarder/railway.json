{
  "$schema": "https://railway.app/railway.schema.json",
  "//": "onboarder 服务的 Railway 配置,与 librarian(读 repo 根 railway.json)并存。新建 service 后,在它的 Settings 里把 'Config file / Railway Config File' 路径指到 /onboarder/railway.json,它就读这份(而非 repo 根那份 librarian 配置)。见 onboarder/README。",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r onboarder/requirements.txt"
  },
  "deploy": {
    "startCommand": "uvicorn onboarder.app:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
