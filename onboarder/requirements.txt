# 接表 agent 依赖。
# 运行时还需要 Claude Code CLI(agent SDK 在底层驱动它):
#   npm install -g @anthropic-ai/claude-code
# 见 .github/workflows/onboard-table.yml。
claude-agent-sdk>=0.2,<1
requests>=2.28
PyYAML>=6
# 可选(仅当接 DB dry-run / agent_runs 记录时):
# supabase>=2,<3
