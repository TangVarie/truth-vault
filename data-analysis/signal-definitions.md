# 信号定义与标签污染实测（2026-09-16）

## 为什么存在

owner 2026-09-16：

> 我觉得我们需要把目前每个项目的 mapping 再细化一下，这里面有些比较关键的地方需要明确一下信号和定义。
> 举个例子，一篇帖子可能标记为爆贴，但其背后的原因是评论数过了 50，但这有可能是我们刷的，所以其真实的流量一般般；
> 又比如，有些帖子我们判断还不错，我们去投了流量，但是投流之后有些涨的可以，说明这个帖子是有潜力的……
> 诸如此类，如果不确定好的话，信号太杂乱了

这份文档把「杂乱」摊开量化。**结论：373 个爆款里 79 个（21%）的标签是被污染的**，
其中只有 10 个（伪爆贴）已经被现有的闸拦住。

> 这不是 mapping 列名写得细不细的问题。是**同一个词「爆」在库里指着四五种不同的东西**。

> 📌 本文 2026-09-16 经 codex PR#128 review 修正四处：⑥ 的「排除」改为只针对特征、
> 补来路分层证据；三/七 的信号搜索 SQL 补上嵌套 `_undeclared`；六 的 flag 由布尔改为
> `routes` 数组并补两列；八 的 AUC 改用中位秩重算。原结论方向均未变。

---

## 〇、2026-09-17 修订：运营对齐回复之后（只读这一节也够）

7 个问题运营全答了（原文照录见 `ops-answers-2026-09-17.md`）。逐条对着库重跑过，本文下面几节里
**3 处前提被推翻、4 处被证实**。被推翻的地方原处加了「⚠️ 修订」标记，这里汇总。

### 被推翻的

**① 「爆」的口径各项目不可比（第③节）—— 错。**
运营：爆贴**只看评论数**，全项目统一：**爆 ≥ 50 / 大爆 ≥ 100 / 评估中 20–50 / 趴 < 20**
（品牌口径是 > 100；内部把 50 设成「起量苗头」，运营自己看大爆）。用有评论数的 5 个项目验：

| 项目 | tier | n | 评论数中位 | 评论数范围 | 达线的 |
|---|---|---|---|---|---|
| ANSHEN | 爆 | 2 | 51 | 51–51 | 2/2 |
| BJS | 爆 / 大爆 | 3 / 1 | 56 / 191 | 50–75 / — | 3/3 · 1/1 |
| SPX | 爆 / 大爆 | 14 / 15 | 62.5 / 144 | 50–98 / 110–418 | 14/14 · 15/15 |
| TUGE | 爆 / 大爆 | 45 / 1 | 51 / 109 | 46–94 / — | 39/45 · 1/1 |
| TGV | 爆 / 大爆 | 9 / 5 | 520 / 1874 | 203–831 / 314–2991 | 全部 |

运营手标的 爆/大爆 与「评论数 ≥50 / ≥100」几乎逐行吻合；趴 全部 < 20
（ANSHEN 173/173、BJS 175/175、SPX 390/392）。**标签本身是可比的。**
第③节那张「爆的互动量中位数」表比的是互动量 —— 而互动量根本不是判爆的依据，它量的是另一件事。
ANSHEN 那一行更是直接错了：ANSHEN 的 2 条爆贴评论数 51、**互动量中位 81.5，不是 1**
（运营指出我用的是旧数据；9/16 之后夜跑刷新过）。

那什么才不可比？**同样 50 条评论背后的真实热度。** 这才是 owner 那个例子的真正形态，见下一条。

**② 「维护评论50条」= 铺评工单（第④节、第六节 route ①）—— 半对。**
运营：那一列是**评论稿的留存**，不是状态列，不标「铺没铺完」；铺没铺完只能看帖子评论数；
铺的评论**算进评论数、也算进互动量**；**只有途鸽大规模铺评**。

所以「有这列 = 铺了」在途鸽成立（它的流程里稿准备好就铺），但 route 的**语义**要改：
它是「铺评已准备」的信号，不是「铺评已完成」的记录。数据重跑（9/17，比 9/16 多了几晚夜跑）：

| TUGE 爆贴 | n | 实时评论数中位 | 互动量中位 | 互动量 < 评论数 |
|---|---|---|---|---|
| 有「维护评论50条」 | 33 | 51 | **7** | 13/33 |
| 没有 | 12 | 54.5 | **156** | 2/12 |
| 评估中 + 有该列 | 35 | 47 | 3 | — |

**途鸽 45 条爆贴里 33 条（73%）是铺够 50 条评论跨过门槛的**，真实互动中位 7。
评估中那 35 条评论数中位 47 —— 它们就是铺评进度条，铺完就翻成爆。
这正是 owner 说的「评论数过了 50 但有可能是我们刷的」，现在有运营口径背书。

「互动量 < 评论数」13/33 说明 `互动量` 不是实时的：实时评论数是巡查机器人抓的当前总数，
互动量是数据回收时的快照。铺评推高了实时评论数，互动量还停在铺评前。
这个时间差反而有用：**实时评论数 ≥ 50 而互动量远低于评论数 = 铺评跨线**。

**③ 「评估中」= 还没下结论、脚本替运营升成爆（第①节、第五节第 2 条）—— 错。**
运营：评估中是**评论数 20–50 的正式档位**，41 条不用动。
那 12 条被数值推断从评估中升成爆的：TUGE 4 条评论数 47/47/49/49（按口径就是评估中）、
WTG 3 条互动量 33/36/46（WTG 阈值 30，没有评论数列）、RIO 3 / XIWU 2 没有评论数列。
**数值推断按互动量判、运营按评论数判，两套尺子。** 见下方「代码侧待定」。

### 被证实的

- **④ 改评发布 / 二次评论是起量后才做的**（第⑥节）。运营确认「绝大多数都是爆贴情况下才做」，
  最准确的含义：原评论不合平台要求无法置顶、换了一条。route ② 起量后干预 语义成立，
  「保留为正例、不当特征」不变。
- **⑤ `实时数据.评论数`**（第⑦节）：机器抓、当前总数、**含铺评**。
- **⑥ 投流全库不存在**（第三节）。运营确认目前只在「维护情况」里记要不要投、几点投；
  后续三个**人工列**（机器不得覆盖）：维护情况（多选：准备/已做投流）、维护时间、维护效果（投流后曝光增长）。
  已声明进 `_template.yaml` 和 ANSHEN/SPX/TUGE/BJS 的 mapping（allowlist → raw_extra 留痕；
  等飞书里真建出来、选项定了再升 typed 列）。
- **⑦ 置顶评论**：填的是「第一屏有没有置顶显示」，有就填，不管起没起量。→ 见「对 D-060 的影响」。

### 新知道的

- **「起量时间」是飞书侧的机器列**：判据「评论数比上次涨 ≥ 50% 且多 ≥ 20 条」；运营想改成
  「评论 ≥ 50 时记录」。**这条自动化不在本仓**（grep 全仓无计算逻辑，mapping 只留痕）——
  改判据要找飞书自动化的维护者。「起量情况」后续不会再出现。
- **`notes` 表没有评论数列。** 判爆的唯一依据在库里只以 raw_extra 键存在
  （`实时数据.评论数`：ANSHEN/SPX/TUGE 顶层、BJS 在 `_undeclared`；`评论数`：TGV），
  其余 11 个项目**根本没有评论数**。

### 对 D-060 的影响

| route | 原判据 | 修订 |
|---|---|---|
| `铺评工单` | `维护评论50条` / `评论铺设情况` 非空 → 指标可能是刷的 | 判据不变，**语义改为「铺评已准备」**；对途鸽等价于已铺（运营确认只有途鸽大规模铺评）。L2 仍剔出正例 |
| `起量后干预` | 控评 / 改评发布·二次评论·改评显示 / 爆帖控评置顶·爆帖置顶评论 | 运营确认为起量后动作，**不变** |
| typed 列 `pinned_comment`（127 行，126 爆） | 未决 | 运营：记的是「有没有置顶显示」。置顶只能作者做、列名就叫「爆帖置顶评论」→ 属起量后干预类，**建议并入 route ②**（不当特征、保留正例）。待 owner 点头再改代码 |

### 代码侧待定（要 owner 拍板 —— 会改看板的爆款数）

1. **数值推断用错了尺子。** 它按 `interactions` 升 tier，运营的标签按评论数。43 条数值推断里
   12 条来自评估中（按口径不是爆）；TGV 14 条全是推断（没人工标），其中 9 条按评论数该是大爆而不是爆。
   改法：有评论数的项目按评论数（≥50 / ≥100）推断，没有的**不推断**；评估中不再升。
   看板影响：TXQ 9→1、RIO 32→23、WTG 3→0、XIWU 11→9、HXZ_QD 6→4、HXZ_FB 15→14、TUGE 46→42，TGV 不变。
2. **「评估中」升成正式 tier 值**（现在映成 未知）。要动 CHECK 约束 + 5 张表的 tier 规则。
3. **加 `comment_count` typed 列**，把 `实时数据.评论数` / `评论数` 接进去 —— 判爆的依据不该只以
   raw_extra 键存在。这是 1 的前提，也让 L2 对途鸽能直接用「评论数 − 互动量」当铺评信号。

---

## 一、四类信号，四种处理原则

| 类 | 是什么 | 能当标签吗 | 能当特征吗 |
|---|---|---|---|
| **结果类** | tier / impressions / reads / interactions | ✅ 这就是标签 | ❌ 发布前拿不到 |
| **内容类** | raw_content / emotional_lever / archetype / audience / format | ❌ | ✅ 发布前就有，无泄漏 |
| **干预类** | 控评 / 铺评 / 置顶 / 改评 / 投流 | ❌ | ⚠️ **多数是后验泄漏** |
| **运维类** | 巡查状态 / 最近检查时间 / 排队刷新 / 连续失败次数 | ❌ | ❌ 纯机器人工单 |

**干预类是最危险的一类** —— 它看起来像特征（发布后就有值），实际是结果的后果。
喂给模型会让 AUC 虚高，上线后一文不值。

---

## 二、逐信号定性

### ① 标签来源：`tier` 的五种来路，可信度完全不同

`tier_source` 已经记了来路，但下游（包括我自己 9/15 的 L2 分析）普遍直接读 `tier` 忽略它。

| tier_source | 行数 | 含义 | 可信度 |
|---|---|---|---|
| 状态字段 | 5,384 | 运营在「流量状态」列手标 | 高（但口径见 ③） |
| 数值推断 | 43 | **运营没标，脚本按阈值自己升的** | 低 |
| 数据异常 | 1 | — | — |
| null | 521 | 没有 tier 源 | — |

**43 条数值推断里 41 条是爆/大爆。** 这些行运营写的是「评估中」——
即「还没下结论」，脚本把它升成了「爆」。RIO 3 / TUGE 4 / WTG 3 / XIWU 2 等。

> ⚠️ **2026-09-17 修订**：「评估中」不是「还没下结论」，是评论数 20–50 的正式档位（运营 Q1/第三部分）。
> 那 12 条升级按运营口径是错的 —— 数值推断按互动量判，运营按评论数判。见 〇·③ 与「代码侧待定」。

→ **口径：L2 训练集必须排除 `tier_source='数值推断'`。** 运营没判的不算判了。

### ② 假爆款闸：`data_quality_flags.synthetic` —— 已存在，但只认两个信号

现有实现（`_SYNTHETIC_TIER_SRC_RE`）认：
- 笔记状态含「关注」（WTG 式）
- tier 源命中 `伪爆[贴帖]|伪\d+评`（RIO 式「伪爆贴500」、BJS/SPX 式「伪爆帖500」）

**这部分工作得很好**：库里「标了伪爆但没标 synthetic」= **0 条**。看板 `v_dash_*` 也已经把它们剔出爆款率。

但它的视野只有这两个信号。爆款里：

| | 条数 | synthetic 认吗 |
|---|---|---|
| 运营手标伪爆贴 | 10 | ✅ 全部拦住 |
| 挂了「维护评论50条」 | 26 | ❌ |
| 纯数值推断升上来的 | 41 | ❌ |
| 互动量 ≤ 本项目「趴」中位数 | 9 | ❌ |
| synthetic 从没被判定过（两个信号都不在场） | 212 | ⚠️ 键都没写 |

### ③ 「爆」的口径在各项目之间不可比

各项目「爆」的互动量中位数：

| 项目 | 爆的中位互动 | 趴的中位互动 |
|---|---|---|
| TGV | 925 | — |
| NRT_2 / NRT_3 | 589 / 526 | 3 / 4 |
| RIO / OKMAN | 466 / 449 | 2 / 4 |
| NUC | 294 | 4 |
| SPX / TUGE | 80 / 52 | 2 / 3 |
| WTG / BJS | 36 / 13 | 1 / 0 |
| **ANSHEN** | **1** | 1 |

**ANSHEN 的「爆」（中位互动 1）比 NUC 的「趴」（中位 4）还差。**

→ **口径：分数、阈值、爆款率永远不能跨项目比。** 只能项目内排序。

> ⚠️ **2026-09-17 修订 —— 这一节的框架错了。** 判爆依据是评论数、全项目统一（爆 ≥50 / 大爆 ≥100），
> 标签本身可比；这张表比的是互动量，量的是另一件事。ANSHEN 那行是旧数据：现为评论数 51、互动量中位 81.5。
> 「不能跨项目比」这个结论对互动量仍成立，但理由变了：同样 50 条评论背后的真实热度不同。见 〇·①。

### ④ 刷评：`维护评论50条` —— owner 那个例子的实证

TUGE 的这一列里存的是**50 条水军评论的完整文案**（一整段，不是计数）。

| TUGE 爆贴 | 条数 | 互动量中位数 |
|---|---|---|
| 挂了「维护评论50条」 | 26 | **1 / 2 / 3 / 6 / 13** |
| 没挂 | 12 | **156** |

同期 TUGE 的「实时数据.评论数」中位 **48**（卡在 50 门槛下沿），而互动量中位 **6**。
评论和互动彻底脱节 —— **评论是铺的，互动是真的**。

> ⚠️ **2026-09-17 修订**：机制被运营确认（爆 = 评论数 ≥50，铺的评论算进评论数，只有途鸽大规模铺评），
> 但那一列的语义是「评论稿留存」而非「铺评完成记录」。数字更新为 33 / 12（多了几晚夜跑）。见 〇·②。

### ⑤ 控评：真爆贴也会控评，不能一刀切

tier 源里出现过：`控评✅`（RIO 9）、`控评&置顶✅`（SPX 2）、`重点控评`（TUGE 1）、`注意控评`（SPX 1）。

RIO 的 9 条控评里 8 条同时是「伪爆贴500」，但 **SPX 的「控评&置顶✅」挂在互动量 136 的真大爆上**。

→ **口径：控评 ≠ 假爆款。** 把控评一律判 synthetic 等于把真赢家删出飞轮。
必须**正交另开一个标记**。这就是 D-060 `comment_maintained` 的由来。

### ⑥ 后验泄漏：`评论状态`

| 评论状态 | n | 其中爆 | 互动中位 |
|---|---|---|---|
| 改评发布 | 24 | **20 (83%)** | **512** |
| 二次评论 | 5 | **5 (100%)** | **362** |
| 改评显示 | 1 | 1 | 1225 |
| 正常发布 | 427 | 88 | 8 |
| 显示评论 | 942 | 66 | 5 |
| 没有显示 | 2,069 | 90 | 3 |

**「改评发布」「二次评论」几乎 100% 命中爆贴** —— 因为只有帖子已经起量，运营才会回去改评/二次铺评。

→ **口径：这三个值是果不是因，不能当【特征】。**
把它喂进 L2，AUC 会好看得离谱，上线归零。

> ⚠️ **但它们不是假爆款，不能当【行】排除。** 这一点第一版写含糊了（写成「主动排除」，
> 读起来像要剔掉这些行），2026-09-16 被 codex PR#128 P1 指出来。查实测，
> 爆款按干预来路分层：
>
> | 来路 | n | p25 | 中位互动 | p75 |
> |---|---|---|---|---|
> | ① 铺评工单（`维护评论50条`/`评论铺设情况`） | 28 | 3 | **13** | 105 |
> | ② 评论状态（改评/二次/改评显示） | 26 | 362 | **1225** | 2134 |
> | ③ tier 源含「控评」 | 13 | 41 | **856** | 865 |
> | ④ 完全没有干预信号 | 306 | 135 | **303** | 731 |
>
> **② ③ 的互动量比「完全没有干预信号」的爆款还高 3~4 倍。** 它们是真赢家 ——
> 运营对已经爆了的帖子做二次运营。整体剔除会删掉 39 条最真的爆款。
>
> 所以 `comment_maintained` 不能是一个布尔，必须**记来路**。见第六节。

### ⑦ 实时 vs 累计：`实时数据.评论数` / `上次评论数`

巡查机器人写的快照对。分布：

| 项目 | 有实时值 | 涨了 | 没动 | 掉了 | 实时评论中位 | 互动中位 |
|---|---|---|---|---|---|---|
| TUGE | 80 | 19 | 56 | 3 | **48** | 6 |
| SPX | 458 | 0 | 29 | 0 | 2 | 2 |
| ANSHEN | 177 | 5 | 172 | 0 | 0 | 1 |
| BJS | 180 | 3 | 177 | 0 | 0 | 0 |

owner 说的「有可能是新刷的也有可能是新涨起来的」—— **只看单次快照区分不了**。
能区分的是差分（实时 − 上次）+ 它和互动量的比值：**评论涨了但互动没涨 = 刷**。
目前只有 TUGE 有 22 条发生过变化，样本太少，先把信号留痕，不下判定。

---

## 三、缺失的信号：**投流数据全库不存在**

owner 提的第二个场景：

> 有些帖子我们判断还不错，我们去投了流量，投流之后有些涨的可以，说明这个帖子是有潜力的，
> 之前只是种种原因被限制了；有些投不出来，说明确实帖子质量就是差

全库 5,949 行扫过一遍 —— **顶层 `raw_extra` 键与嵌套 `raw_extra->_undeclared` 键并集**，
匹配 `投流|薯条|加热|推广|助推|千川|付费|投放|流量|dou`：

| 沾边的列 | 行数 |
|---|---|
| 起量时间 | 48 |
| 起量情况 | 29 |
| **投流相关列** | **0** |

> 第一版这里只扫了顶层键，codex PR#128 P1 指出：被 D-055 吸收的列在顶层只表现为
> 字面键 `_undeclared`，真实列名藏在下一层，所以一个未声明的「投流」列会完全隐形。
> 这条说得对。已用并集重跑（SQL 见第七节），**结果仍为空**，结论不变。

**这个维度飞书表里就没有。** 不是 mapping 没接，是运营根本没在记。

这也是所有信号里**信息量最大的一个** —— 它能把「内容差」和「内容好但没投出去」分开，
而这正是现在 AUC 卡在 0.6 的核心原因之一：模型看不到「这条根本没获得曝光机会」。

→ **需要运营侧新增**，见第五节。

---

## 四、已证伪的两个担心（别再查了）

### ① 「副本」列 ≠ 另一份数据

`互动副本`/`阅读副本`/`曝光副本`/`互动量副本`… 共 8 个项目 3,268 行。
与主值逐行比对：**3,260 行完全相等**，7 行不等（全在 RIO），1 行只有副本。

→ **纯复制列，永久忽略。**

### ② 「同一份文案铺多个账号」不是 SPX 反常的原因

我一开始猜 SPX 正文模型反向（AUC 0.232）是因为素人分发导致同文案不同结果。查下来不成立：

| 项目 | 文案重复率 | 正文模型 AUC |
|---|---|---|
| RIO | **27.8%** | 0.626（好） |
| SPX | **3.9%** | **0.320（反向）** |
| 其余 13 个 | ≈0% | — |

重复率最高的 RIO 正文模型反而是好的。SPX 的真实原因是**正文长度**：
SPX 的爆贴正文中位 **118 字**，趴贴 **253 字** —— 爆的比趴的短一半。
其它项目没有这个规律，跨项目学来的权重在 SPX 上系统性反向。

---

## 五、要运营做的事（按收益排序）

| # | 做什么 | 为什么 | 影响面 |
|---|---|---|---|
| 1 | **加投流列**：是否投流 / 投流金额 / 投流后增量 | 现在完全看不到「有没有给过曝光机会」，无法区分「内容差」和「没投出去」 | 全部项目 |
| 2 | 「评估中」不要停在那儿 | 41 条爆款是脚本替运营下的结论 | 有 tier 源的表 |
| 3 | 铺评的帖子在状态里明确标 | 现在只有 TUGE 能看出来，靠的还是一个存文案的列 | 全部项目 |
| 4 | ANSHEN 的「爆」口径对一下 | 中位互动 1，比别的项目「趴」还低 | ANSHEN |

> ⚠️ **2026-09-17 回复状态**：1 → 运营改用自己的三个列（维护情况/维护时间/维护效果），已声明；
> 2 → **撤回**，评估中是正式档位，错的是数值推断；3 → 运营问「需要我标吗」，回复要（只途鸽，一个完成标记）；
> 4 → **撤回**，是我用了旧数据。见 〇 节与 `ops-reply-2026-09-17.md`。

**不需要运营做的**：SPX 的抖音行（owner 已定不管）、副本列、`title`/`body` 拆分
（schema 有这三列但全空，正文都在 `raw_content`，260 字中位 —— 现阶段不拆也够用）。

---

## 六、代码侧已经改了什么

**D-060 `data_quality_flags.comment_maintained`**（本次）—— 与 `synthetic` **正交**的新标记。

为什么不并进 `synthetic`：见 ⑤。`synthetic` 被 20 多处读（看板 `v_dash_*`、飞轮通道 1 ssll、
馆员、ssll 回灌），语义一动全身动，而且并进去会误删真赢家。

**记的是 `routes` 数组，不是一个布尔** —— 因为两条来路语义相反（见 ⑥ 的分层表）：

| route | 触发条件 | 含义 | L2 该怎么用 |
|---|---|---|---|
| `铺评工单` | 工单列 `维护评论50条` / `评论铺设情况` 非空 | 指标可能是刷出来的（中位互动 13；9/17 重跑 7）。⚠️ 9/17 修订：语义是「铺评已准备」，对途鸽等价于已铺 | **剔出正例** |
| `起量后干预` | tier 源含「控评」<br>`评论状态` 含 `改评发布`/`二次评论`/`改评显示`<br>列 `爆帖控评置顶` / `爆帖置顶评论` 非空 | 指标是真的，但含人工成分（中位互动 856~1225） | **保留为正例，但别当特征** |

布尔 `comment_maintained` 保留（= 任一来路命中），向后兼容。

`爆帖控评置顶`（HXZ_QD 3 行）和 `爆帖置顶评论`（HXZ_FB 9 行）是 codex PR#128 P2 指出的 ——
第一版完全没收这两列，3 行全漏、9 行漏 5 行。

**未决**：typed 列 `pinned_comment` 有 **127 行非空，其中 126 行是爆款**，形态与「起量后干预」
这一组一致，但「记下置顶的是哪条评论」和「做了置顶这个动作」是两回事，**从数据里分不出来**。
已列进给运营的待确认清单，确认前不猜、不收。

> ⚠️ **2026-09-17 运营回复**：填的是「第一屏有没有置顶显示」，有就填、不管起没起量。
> 置顶只能作者做、列名就叫「爆帖置顶评论」→ 建议并入 route ② 起量后干预。待 owner 点头再改代码。

**三路都不在场时缺键，不写 false** —— 一张表没有控评列，不等于它的帖子没被控评。
写 false 是拿「假阴性」冒充「已核实」。缺键让下游能区分「没被控评」和「不知道」。

未声明列（D-055 `_undeclared`）路径同样要认 —— BJS/SPX 的「评论状态」就是未声明的，
不走这条路会导致同一个运营动作因为「这张表声明没声明」而时有时无。

守卫：CI 第 ⑦ 组 7 个用例，5 处反证全部变红。

---

## 七、复现 SQL

### 标签污染盘点

```sql
WITH pa AS (
  SELECT project_id, percentile_disc(0.5) WITHIN GROUP (ORDER BY interactions) pm
  FROM truth_vault.notes WHERE tier='趴' AND interactions IS NOT NULL GROUP BY 1
), b AS (
  SELECT n.*, pa.pm, (n.data_quality_flags->>'synthetic')::bool syn
  FROM truth_vault.notes n LEFT JOIN pa USING (project_id) WHERE n.tier IN ('爆','大爆')
)
SELECT count(*) 爆款总数,
  count(*) FILTER (WHERE syn IS TRUE) 已标synthetic,
  count(*) FILTER (WHERE syn IS NULL) 从没判定过,
  count(*) FILTER (WHERE COALESCE(raw_extra->>'_tier_source_raw','') LIKE '%伪爆%' AND syn IS NOT TRUE) 伪爆漏标,
  count(*) FILTER (WHERE (raw_extra ? '维护评论50条') AND syn IS NOT TRUE) 刷50评没标,
  count(*) FILTER (WHERE tier_source='数值推断' AND syn IS NOT TRUE) 数值推断没标,
  count(*) FILTER (WHERE interactions<=pm AND syn IS NOT TRUE) 低于趴中位没标
FROM b;
```

### 各项目「爆」的口径

```sql
SELECT project_id,
  percentile_disc(0.5) WITHIN GROUP (ORDER BY interactions)
    FILTER (WHERE tier IN ('爆','大爆')) 爆的中位互动,
  percentile_disc(0.5) WITHIN GROUP (ORDER BY interactions)
    FILTER (WHERE tier='趴') 趴的中位互动
FROM truth_vault.notes WHERE interactions IS NOT NULL
GROUP BY 1 HAVING count(*) FILTER (WHERE tier IN ('爆','大爆')) > 0
ORDER BY 2 DESC NULLS LAST;
```

### 后验泄漏字段

```sql
SELECT left(COALESCE(raw_extra->>'评论状态', raw_extra->'_undeclared'->>'评论状态'),20) 评论状态,
       count(*) n, count(*) FILTER (WHERE tier IN ('爆','大爆')) 其中爆,
       percentile_disc(0.5) WITHIN GROUP (ORDER BY interactions) 互动中位
FROM truth_vault.notes
WHERE COALESCE(raw_extra->>'评论状态', raw_extra->'_undeclared'->>'评论状态') IS NOT NULL
GROUP BY 1 ORDER BY n DESC;
```

### 全库找某类信号在不在

**必须并上嵌套 `_undeclared`** —— 只扫顶层会把所有被 D-055 吸收的列看成字面键
`_undeclared`，真实列名完全隐形：

```sql
WITH allk AS (
  SELECT project_id, k FROM truth_vault.notes,
         jsonb_object_keys(COALESCE(raw_extra,'{}'::jsonb)) k
  UNION ALL
  SELECT project_id, k FROM truth_vault.notes,
         jsonb_object_keys(COALESCE(raw_extra->'_undeclared','{}'::jsonb)) k
)
SELECT k, count(*) n, string_agg(DISTINCT project_id, ',') projs
FROM allk
WHERE k ~ '投流|薯条|加热|推广|助推|千川|付费|投放|流量|dou'   -- 换成你要找的信号
GROUP BY 1 ORDER BY n DESC;
```

---

## 八、L2 训练集的口径（结论）

基于以上，L2 的正例定义应当是：

```sql
tier IN ('爆','大爆')
AND tier_source <> '数值推断'                                    -- ① 运营没判的不算
AND COALESCE(data_quality_flags->>'synthetic','false') <> 'true'  -- ② 已有的假爆款闸
AND COALESCE(raw_extra->>'_tier_source_raw','') NOT LIKE '%伪爆%' -- ③ ②的冗余兜底
AND NOT (raw_extra ? '维护评论50条' OR raw_extra ? '评论铺设情况') -- ④ 铺评工单(= route 铺评工单)
AND interactions > (本项目 '趴' 的互动中位数)                      -- ⑤ 内部自洽
```

**④ 只剔「铺评工单」这一条 route，【不剔】「起量后干预」。**
codex PR#128 P1 建议把 `comment_maintained=true` 整体剔掉 —— 那是错的，会删掉 39 条
互动中位 856~1225 的真爆款（比无干预信号的爆款还高 3~4 倍）。依据见 ⑥ 的分层表。

按这个口径：**373 → 294 个正例**。
留一项目交叉验证 AUC 从 **0.622**（污染标签）变成 **0.630**（清洗后）。

> 这两个数是 2026-09-16 用**中位秩**重算的。第一版用 PostgreSQL `rank()`，
> 它给并列行的是最小秩而非平均秩，而 Mann–Whitney 公式要求平均秩 —— codex PR#128 P2
> 指出的真 bug。并列很多（SPX 最大并列 44 行、NUC 24 行），修正后两个数都上移约 0.01，
> 清洗的增益从 +0.012 变成 +0.008。详见 [l2-feasibility.md](l2-feasibility.md) 第八节。

**AUC 只涨了 0.008，但那 0.008 不是重点** —— 重点是之前每 5 个正例里有 1 个是假的，
任何基于它的判断都在被误导。
