{
  "$schema": "https://railway.app/railway.schema.json",
  "//": "worker 服务的 Railway 配置,与 librarian(读 repo 根 railway.json)、onboarder(/onboarder/railway.json)并存。新建 service 后,在它的 Settings 里把 'Config file / Railway Config File' 路径指到 /worker/railway.json,它就读这份。见 worker/README。",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r worker/requirements.txt"
  },
  "deploy": {
    "startCommand": "uvicorn worker.app:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
